package method;

public class returnTypeObject {
    int max(int x,int y)
    {
    return x>y?x:y;
    }
    String userName(String email)
    {
        int a=email.indexOf("@");
        String name=email.substring(o, a);
        return name;
    }
    
}
